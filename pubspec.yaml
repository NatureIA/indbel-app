name: indbel_app
description: App da Incobel com login via CSV online
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/dados.csv
